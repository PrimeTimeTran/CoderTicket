<div class="container">
  <h2>Contextual Classes</h2>
  <p>Contextual classes can be used to color table rows or table cells. The classes that can be used are: .active, .success, .info, .warning, and .danger.</p>
  <table class="table">
    <thead>
      <tr>
        <th>Name</th>
        <th>Address</th>
        <th>Region</th>
        <th>Edit Venue</th>
      </tr>
    </thead>
    <tbody>
      <% @venues.each do |v| %>
      <tr class="warning">
        <td><%= v.name %></td>
        <td><%= v.full_address %></td>
        <td><%= v.region_id %></td>
        <td><%= link_to "Edit", edit_venue_path(v), class: "btn-warning" %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
  <%= link_to "Create Venue", new_venue_path, class: "btn btn-success" %>
</div>
